name: Chat Dev Reasoning Brake v0 (Retired Windows Lane)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired production path
        run: |
          echo "This public CODEX-BRAKE-V0 -> Windows runner lane is retired."
          echo "Active Reasoning Brake execution is private ga815647/chatdev-exec."
          echo "Do not use this workflow as fallback."
