# Agent Prompt 系統性 Review 流程

適用場景：寫完或改完一整份 Agent 的系統指令（System Prompt），在實測前做最後檢查。

---

## 每輪必問（三題）

每次改完 prompt，貼上 prompt 全文，後面接這段：

```
請針對這份 Agent Prompt 依序回答三個問題：

①　現在這份 prompt，如果 agent 執行錯誤，使用者有辦法立刻發現嗎？具體指出哪些錯誤會靜默發生。
②　你還能指出「哪一段指令」會導致 agent 在「什麼具體情境」下做錯事嗎？請像 code reviewer 一樣引用原文並說明。
③　綜合以上，你覺得繼續修改這份 prompt 的收益，跟直接讓 agent 跑一次看實際結果相比，哪個更高？

如果第②題已經指不出具體問題，請直接告訴我：「建議實測」。
如果第②題指出的問題嚴重度都在「不影響核心流程正確性」的等級，也請告訴我：「建議實測，剩餘問題不值得在紙上修」。
```

---

## 每三輪加問（瘦身檢查）

```
⓪ 如果我現在必須砍掉這份 prompt 20% 的字數，你會砍哪些段落？砍完後 agent 的核心行為會受影響嗎？
  （砍字數時優先考慮：能不能用一個範例取代一整段規則。）
```

---

## 各階段預判與處理

### 第 1～2 輪：問題又多又具體

**會發生什麼：**
LLM 在①②兩題都會給出大量具體問題。靜默錯誤通常集中在：agent 選錯分支但沒有 log、API 回傳空值但 agent 當成功處理、格式對了但內容錯了使用者看不出來。第③題會明確告訴你「繼續改」。

**怎麼處理：**
只修第②題指出的問題。第①題的靜默錯誤先記下來，但不要急著全塞進 prompt——有些靠 prompt 解決，有些應該靠外部 logging 解決。每次只改一個方向，不要同時修三四個問題，否則改壞了不知道是哪個動的。

---

### 第 3～4 輪：問題變少，開始挑小毛病

**會發生什麼：**
第②題還是能指出問題，但嚴重度明顯下降——「這段措辭可能讓 agent 誤解」「這個邊界案例理論上可能出錯」。第③題開始搖擺，說「收益有限但還有空間」這種模稜兩可的話。

**怎麼處理：**
這是最危險的階段，你會想「再改一版應該更好」。看第②題的問題：如果它影響的情境在你的實際使用中根本不會出現，不要改。判斷第③題是否該停的標準：如果 LLM 的回答出現「收益有限」「邊際改善」「理論上可能」這類降級詞彙，而不是明確說「應該繼續改」，就傾向直接實測。

**如果你的工具有 plan mode（如 Claude Code 的 plan mode、Codex 的 plan-only 模式）：** 這個階段可以更果斷地選擇直接實測。Plan mode 讓 agent 先輸出計畫而不執行，你看完再決定放不放行。等於實測多了一道安全網——就算 prompt 有殘留問題，你在執行前還來得及攔。

---

### 第 3 輪（觸發⓪題）：瘦身檢查

**會發生什麼：**
LLM 會列出可以砍的段落，但通常會加一堆「不過這段在某些情況下可能有用」的但書來對沖。

**怎麼處理：**
看它列出的段落，如果你自己也記不起來當初為什麼加那段，直接砍。如果它建議用一個範例取代一整段規則，讓它當場改寫，你比對一下效果。砍完之後回到三題流程重新跑一輪，確認沒有砍壞。

---

### 第 5 輪以後：收到「建議實測」

**會發生什麼：**
第②題答不出具體問題，或只剩下「不影響核心流程」的小事。LLM 回覆「建議實測」或「建議實測，剩餘問題不值得在紙上修」。

**怎麼處理：**
收工。接下來轉入實測階段，拿真實輸入跑 agent，觀察實際行為。實測中發現的問題才是下一輪修改的依據，不要在這個階段繼續跟 LLM 紙上談兵。

---

### 大型任務建議加做：沙盤推演

收到「建議實測」後、正式跑 agent 前，對 LLM 問一句：

```
模擬這個任務的第一階段，列出最可能出現的問題。
```

**為什麼只有大型任務需要：**
大任務跑一次成本高（時間、token、debug），提前擋掉一兩個坑划算。小任務直接跑比較快，出了問題當場修就好。

**怎麼判斷要不要做：**
如果這次實測失敗後你需要花超過 30 分鐘重來，就值得先模擬。

---

### 異常狀況：超過 6 輪還沒收到「建議實測」

**會發生什麼：**
LLM 每次都能從第②題擠出新問題，但你感覺都是換個角度說同一件事，或者問題越來越牽強。

**怎麼處理：**
強制觸發⓪題做一次瘦身，然後無論結果如何，直接進實測。超過 6 輪還在紙上改，幾乎可以確定你已經過了邊際效益的拐點。

---

## 流程圖

```
改完 prompt
    │
    ▼
┌─────────────────────────┐
│  貼 prompt + 問 ①②③     │◄──────────────────┐
└─────────────────────────┘                    │
    │                                          │
    ▼                                          │
  已經第 6 輪以上？                              │
    │                                          │
   是──► 強制問 ⓪ 瘦身 ──► 直接實測             │
    │                                          │
   否                                          │
    │                                          │
    ▼                                          │
  ②有具體問題？                                │
    │                                          │
   是──► 問題影響核心流程？                     │
    │       │                                  │
    │      是──► 修改 prompt ──► 已滿 3 輪？    │
    │       │                      │           │
    │       │                     是──► 問 ⓪   │
    │       │                      │    瘦身   │
    │       │                     否 ──────────┘
    │       │                      │
    │      否──► 「建議實測」        │
    │                              │
   否──► 「建議實測」         回到 ①②③
    │                              │
    ▼                              │
  ┌──────┐                         │
  │ 實測  │    發現新問題 ──────────┘
  └──┬───┘
     │
     ▼
   大型任務？──是──► 先沙盤推演 ──► 再正式跑
     │
    否──► 直接跑
```
