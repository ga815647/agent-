# Agent 指令確認規則

讓 agent 收到指令時自動檢查，不需要人工介入。根據需求選擇放置位置。

---

## 三種放法

### 1. 全域（所有專案生效）

檔案位置：`~/.codex/AGENTS.md`（Codex）、`CLAUDE.md`（Claude Code）、`.cursorrules`（Cursor）

```markdown
## 指令確認

- 收到指令後，先判斷是否有模糊或矛盾之處。如果有，先向使用者確認再執行，不要自行假設。
- 如果指令涉及刪除檔案、覆寫資料、或修改 config，執行前先列出影響範圍，等使用者確認。
```

適合：大部分專案都涉及有風險的操作。
代價：所有專案的 agent 都會變謹慎，小實驗可能嫌煩。

---

### 2. 專案層級（單一 repo 生效）

檔案位置：repo 根目錄的 `AGENTS.md`

```markdown
## 指令確認

- 本專案的核心邏輯資料夾（列出路徑）禁止自動修改。收到相關指令時，先輸出計畫，等確認後再動。
```

例如交易系統的寫法：

```markdown
- 本專案的 strategies/、signals/ 禁止自動修改。收到相關指令時，先輸出計畫，等確認後再動。
```

適合：特定專案有高風險區域需要保護。
代價：只保護這個 repo，新專案要記得另外加。

---

### 3. 單次 task（只對這次指令生效）

直接在 task prompt 最前面加一行：

```
在執行任何動作前，先判斷我的指令是否有模糊或矛盾之處。如果有，先列出你的疑問，等我確認後再動。
```

適合：臨時想讓 agent 謹慎一點，不想動設定檔。
代價：這次 task 結束就消失，下次要重新加。
